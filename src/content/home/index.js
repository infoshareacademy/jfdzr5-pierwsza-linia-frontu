import Typography from "@mui/material/Typography";
import {PageWrapper} from "../../common/page-wrapper";

export const Home = () => {
    return <PageWrapper title="Home App">
        <Typography variant="subtitle1">Welcome to our home app!</Typography>
    </PageWrapper>
}